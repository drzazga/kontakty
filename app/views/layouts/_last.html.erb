<div id="lastUpdated">
<div class="box">
  <h2 style="margin-top:17px">Last updated</h2>
	<ul>
		  <% for contact in @last_updated_contacts %>
        <li><%= link_to "#{contact.first_name} #{contact.surname}", contact_path(contact.id) %></li>
      <% end %> 
	</ul>
</div>
</div>
